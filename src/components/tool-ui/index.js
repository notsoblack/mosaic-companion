/**
 * Tool UI — Public API
 *
 * Import from here when you need to render tool UI blocks.
 *
 * Usage:
 *   import { ToolUIRenderer } from "../tool-ui";
 *   <ToolUIRenderer blocks={result.ui} />
 */
export { ToolUIRenderer } from "./ToolUIRenderer";
