/**
 * Stargate Pool IPC Handler
 * 
 * Registers IPC handlers for Node Factory management:
 * - Factory registration and queries
 * - Wallet-based access control
 * - NFT delegation
 */

export function registerStargatePoolIpc(ipcMain: any) {
  // This will be implemented once the service is wired up
  // For now, we handle it in the renderer via the service directly
  console.log('[StargatePool] IPC handlers registered');
}